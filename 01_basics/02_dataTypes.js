"use strict"; //treat all js code as newer version

alert(3+3); /*we are using node js not browser so error will happen. 
            but there is another method in node js that we will
            learn later on*/

console.log(3+
    3); //code readability should be the priority. don't use these kind of style. just because it can be done doesn't mean that it should be done.


let name="Nirmalya"
let age= 19
let isLoggedIn=false;


/*Types of data types:
number=36
bigint(for large numbers)
string="Hello"
boolean=true/false
null = standalone value
undefined
symbol = for uniqueness


object*/

console.log(typeof age);